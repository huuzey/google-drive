interface Button {
  btnclass?: string;
  title: string;
  onclick?: () => void;
}
interface GithubAuth {
  clientId: string;
  clientSecret: string;
}
interface progress {
  progress: number;
}
interface Arraytype {
  map: Function;
}
interface uploadfileinter {
  parentid?: string;
}
interface fileuploading {
  file: any;
  parentid: [];
  setprogress: Function;
}
